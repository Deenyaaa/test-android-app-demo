# VPN App Speed Test

**VPNAppTestSpeed** — это проект для автоматизированного тестирования производительности VPN-серверов с использованием мобильного приложения VPN и Speedtest на Android. Тесты написаны на Python с использованием **Pytest** и **Appium**.

Проект позволяет подключаться к каждому серверу VPN, измерять скорость соединения и пинг, а результаты логируются и сохраняются в отчёт.

---

## 📦 Структура проекта

```
VPNAppTestSpeed/
│
├── tests/                       # Тестовые сценарии
│   ├── test_servers.py
│   └── ...
├── pages/                       # Page Object модели
│   ├── vpn_app_main_page.py
│   ├── vpn_app_servers_list_page.py
│   └── speedtest_app_main_page.py
├── logs/  
├── reports/  
├── utils/                       # Утилиты (wait_helpers, reporter)
├── models/                      # Модели данных (Report)
├── conftest.py                  # Фикстуры Pytest
├── pytest.ini                   # Настройки Pytest, константы проекта
└── README.md
```

---

## ⚙️ Предустановки

Перед запуском тестов необходимо:

1. **Python 3.10+**  
   [Скачать Python](https://www.python.org/downloads/)

2. **Appium Server 2.x**  
   [Инструкция по установке Appium](https://appium.io/docs/en/about-appium/intro/)

3. **Android SDK**
   - [SDK Platform Tools](https://developer.android.com/tools/adb)  
   - Emulator или реальное устройство с включенной отладкой USB  
   - Рекомендуемая версия Android: 15

4. **Python-зависимости проекта**
   ```bash
   pip install -r requirements.txt
   ```

---

## 🚀 Запуск тестов

### 1. Подключение Appium и устройства

- Запустите Appium Server:  
  ```bash
  appium --use-plugins=inspector --allow-cors
  ```
- Подключите Android-устройство или эмулятор.

### 2. Запуск теста напрямую

```bash
pytest -v -s tests/test_servers.py
```

- `-v` — подробный вывод  
- `-s` — позволяет видеть `print()` в консоли

### 3. Запуск всех тестов

```bash
pytest
```

- Все тесты из папки `tests/` будут выполнены

---

## 📝 Конфигурация

Основные параметры (конфигурация pytest, пакеты приложений, device и версия Android) можно изменить в файле `pytest.ini`:

---

## 📊 Отчёт

После выполнения теста создаётся отчёт с результатами измерений для каждого VPN-сервера в директории reports/.

---
